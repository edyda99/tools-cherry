# feat/authority-moves, ship report

Branch `feat/authority-moves` in `/private/tmp/claude-501/-Users-edmonddaher-Documents-utility-portfolio/d9e2ad41-8d38-4248-8fab-b26368c70c8c/wt-authority`, forked from `main` at `b903205`. Nothing pushed, nothing deployed.

## 1. Verdict

**No, not safe to ship as-is.** Four things block it:

| # | Blocker | Introduced by this branch? |
|---|---|---|
| B1 | Ohio state tax is understated by a flat $342 per filer ($26,050 to $100,000 taxable) because the engine models only the marginal rates and drops the base amount Ohio's own schedule adds. The new page publishes Ohio at rank 11. | No, pre-existing in `src/data/tax-data-2026.json`. The new page republishes it as a ranked authority claim. |
| B2 | The mandatory, uncapped 0.58% WA Cares employee premium is missing from `washington.employeePrograms` ($580/yr at $100,000). The new page publishes Washington at rank 10 and builds a whole paragraph and FAQ on Washington's position. | No, pre-existing. `src/data/state-payroll-2026.json` already documents WA Cares, so the paycheck engine's data file is the one out of date. |
| B3 | URL cannibalization: `/take-home-pay-by-state/` (new) versus `/data/take-home-pay-by-state/` (already live, already in sitemap, already linked from the homepage). Both indexable, both self-canonical, both linked from all 51 state pages. Needs an explicit human call: ship as a pair, or fold one into the other. | Yes. |
| B4 | Three false or self-contradicting copy claims on the new page: "it updates itself when a state publishes new tax brackets" (nothing in the repo ingests state tax data); "the money that actually reaches your bank account"; and "the tax half of the question is already settled" contradicted by the page's own FAQ. | Yes. |

Everything structural passed: build gate, tests, AdSense invariant, shared-CSS invariant, blast radius, mobile layout, SEO plumbing, and 255 of 255 published figures reproduced by an independent engine.

## 2. What shipped

| Change | Files | Commit |
|---|---|---|
| All 51 state paycheck titles and h1s rewritten from generation code. Title base `{State} Paycheck Calculator {year}` plus the longest clause fitting 60 decoded chars. h1 is `{State} Paycheck Calculator` for all 51. Hardcoded `og:title` deleted so `injectSeo` derives og/twitter title from the real `<title>`. | `build.js`, `src/templates/state-page.html` | `3da743e` |
| New page `/take-home-pay-by-state/`: 51-row ranked table, $100,000 salary, single, biweekly, 2026. Every figure computed at build time from `computePaycheck` + `tax-data-2026.json`. Cross-linked from all 51 state pages and from `/data/take-home-pay-by-state/`. Kept out of `TOOLS` (deliberate: TOOLS membership rebuilds the content-hashed Cmd+K index in every page head), added via `RELATED_OVERRIDES` and `DATED_TAX_TOOLS`. | `src/templates/take-home-pay-by-state.html` (new), `build.js`, `src/templates/state-page.html` | `1be1fa1` |
| HN technical writeup, 1,381 words. Not published, not submitted, no commit, untracked. | `docs/hn-pdf-converter-writeup.md` | uncommitted |
| Ten .edu nudge drafts. Drafted only, none sent. | `docs/edu-nudge-drafts.md` | uncommitted |
| Bing / AI-answer-engine surface audit. No site code changed. | `docs/bing-ai-surface-audit.md` | uncommitted |

Title examples, before and after:

| State | Before | After | Len |
|---|---|---|---|
| texas | Texas Paycheck & Payroll Calculator 2026 (TX) | Texas Paycheck Calculator 2026: Take-Home Pay After Taxes | 57 |
| california | California Paycheck Calculator 2026 (CA) [em dash] Tax 1%-12.3% | California Paycheck Calculator 2026: 1%-12.3% Income Tax | 56 |
| district-of-columbia | (old shape) | District of Columbia Paycheck Calculator 2026: Take-Home Pay | 60 |

Title variant split: 30 states get `: Take-Home Pay After Taxes`, 16 long-name states get the shorter `: Take-Home Pay`, 5 near-page-1 states (california, colorado, massachusetts, new-mexico, pennsylvania) keep a rate clause instead. 13 states land at exactly 60 chars, so adding one character to the shared pattern would cost all 13 their whole clause.

## 3. Verification results

Ten passes ran. None returned null or empty. Every one reported.

| Verifier | Verdict | Single most important measurement |
|---|---|---|
| numbers | pass | Independent model (no import of `computePaycheck`) reproduced 255 of 255 published figures across all 51 rows, 5 fields each, exact. Spread $8,777.25, 8-way tie at the top, Oregon at the bottom, all confirmed. |
| sources | **fail** | Primary sources pulled for CA, OR, NY, WA, OH. CA brackets, CA SDI 1.3%, OR brackets and Paid Leave 0.6%, all 27 NY thresholds, NY PFL 411.91, NY DBL 0.60/wk, WA PFML 0.0080716, all confirmed exact. Two blockers found: Ohio missing a $342 base amount, Washington missing WA Cares 0.58%. |
| invariants | pass | AdSense loader count baseline 217 to new 218, delta exactly +1, and the +1 is exactly the new page. 0 `<ins class="adsbygoogle">` in both builds. External hosts the browser contacts: 2 in baseline, the same 2 in new. `ads.txt` md5 identical. |
| blast | pass | Byte-level `diff -rq` over the whole dist: exactly 54 paths differ (51 state pages, `/data/take-home-pay-by-state/`, `sitemap.xml`, the new directory). `dist/assets/` byte-identical, so styles.css and the Cmd+K index were not rewritten. 0 diffs in meta description, canonical or robots on any page. |
| css | pass | `src/assets/styles.css` byte-identical to baseline, md5 `d2432b0556ac0b741251662791cd8ebf` on both, hashed output `styles.81ca39ca81.css` same md5. All 30 new selectors match exactly 1 page each out of 243. Matcher validated against controls (body 243, .wrap 219, table 136, table.thp 1), so the counts of 1 are a finding, not a fallback. |
| gate | pass | `npm run build` exit 0 twice. `npm test` exit 0, 813 `ok -` lines, 15 engine suites at 0 failed. verify-dist: 242 pages, 218 with loader, 24 without. |
| seo | pass | 243 pages, 243 titles, 0 duplicates site-wide. 0 indexable pages over 60 decoded chars (the 11 over-length titles are all `/embed/` widgets, excluded from the sitemap). 51 of 51 states carry the exact `<state> paycheck calculator` phrase. 540 JSON-LD blocks parse, 0 failures. |
| mobile | pass | Real headless Chrome over CDP at 360px: `documentElement.scrollWidth` 360 = `clientWidth` 360, 0px body overflow, `window.scrollX` 0 after both programmatic scroll and synthetic wheel. Table scrolls inside `.thp-scroll` (clientWidth 330, scrollWidth 812) with `window.scrollX` unmoved. Also clean at 320px and 1200px. |
| refutation, numbers lens | **fail** | Cross-page agreement test: for all 51 states, ran the payload the shipped state page actually embeds against the new page's cells. 51 states, 0 mismatches. Then found the page asserts 2026 rules three times while California and Oklahoma carry `figureYear: 2025` with no disclosure. |
| refutation, copy lens | **fail** | Quantified the ranked-table distortion from the undisclosed local-tax omission using the repo's own data: Maryland shown 35th at $74,642 becomes ~$71,442 (50th of 51) at the 3.20% rate the repo documents for its largest counties. Ohio shown 11th becomes 35th at Columbus 2.5%. |

## 4. Every failure and refutation finding

Ordered blocker, major, minor. Nothing omitted.

| Sev | Source | Finding | Evidence | New in this branch? |
|---|---|---|---|---|
| blocker | sources | Ohio engine drops the flat base amount Ohio's schedule adds on top of 2.75%. Understates Ohio tax by $342 for every filer with taxable income $26,050 to $100,000, and $548 at $150,000. | Ohio Dept of Taxation Annual Tax Rates: "$26,050 - $100,000  $342 + 2.75% of excess over $26,050". Our file: `brackets [{rate:0, upTo:26050},{rate:0.0275, upTo:null}]`, no base. Engine at $70,000: ours 1208.63, official 1550.63. Our own `_source` calls it a "rounding offset", which it is not. Even our claimed 2026 statute text says "$332.00 plus 2.75%" while we model $0. | No |
| blocker | sources | WA Cares Fund, 0.58% of gross, employee-paid, no wage cap, is absent from `washington.employeePrograms`. | paidleave.wa.gov: "The WA Cares premium rate is 0.58 percent of each employee's gross wages", "paid by the employee", "there is no Social Security cap". `stateEmployeePrograms(70000, states.washington)` returns only `[{"label":"WA PFML","annual":565.01}]`. Missing $406/yr at $70k, $580/yr at $100k. Internally inconsistent: `src/data/state-payroll-2026.json` already carries WA Cares. | No |
| major | seo | `/take-home-pay-by-state/` cannibalizes `/data/take-home-pay-by-state/`. Both indexable, both in sitemap, both self-canonical, both linked from all 51 state pages. | New page title "Take-Home Pay by State 2026: $100,000 Salary Compared"; existing "Take-Home Pay on $75,000 in All 51 States, Compared (2026)". Link split: 52 pages link to the new one, 54 to the `/data/` one. Mitigating: 8-gram Jaccard 0.019, containment 0.044 over `<main>` text, and the two cross-link each other. Differentiated content at colliding URLs, not duplicate content. | Yes |
| major | sources | Ohio `figureYear` asserts 2026, but Ohio has published no 2026 schedule and its only published table (2025) contradicts ours. No user-facing disclaimer, unlike California's. | tax.ohio.gov: "The following are the Ohio individual income tax brackets for 2005 through 2025", most recent table still carries the 3.125% band we removed. Our sole citation `codes.ohio.gov/.../5747.02` was unreachable. Our own `_source` concedes the 2026 IT 1040 ES worksheet still prints the pre-HB 96 ladder. California, by contrast, sets `figureYear 2025` and shows a visible banner. | No |
| major | sources | Oregon statewide transit tax, 0.1% employee-paid on all wages, no cap, missing from `oregon.employeePrograms`. | oregon.gov/dor: rate "one-tenth of 1 percent or .001", imposed on employee wages. `stateEmployeePrograms(70000, states.oregon)` returns only OR Paid Leave $420. Missing $70/yr at $70k, $100/yr at $100k. Also absent from `state-payroll-2026.json`. | No |
| major | refute/numbers | The page asserts 2026 rules three times, but California and Oklahoma are computed from 2025 figures, and this is the only surface in the site that ships no prior-year disclosure. | `figureYear = 2025` for california and oklahoma. Page copy: template lines 62, 75, 117. California is not a footnote, it is named in the analysis block and ranked 50th with $5,208 of state tax from 2025 brackets and a 2025 $5,706 standard deduction. The `/data/` sibling auto-emits `priorYearNote` (build.js:5375-5381) and state pages render `figureYearBanner` (build.js:1374). The new page block (build.js:5556-5860) has no equivalent. | Yes (the omission of the disclosure) |
| major | refute/numbers | The ranking rests on a state layer that ignores personal exemptions, credits and federal-tax subtractions for 30 of 51 jurisdictions, unevenly, and the page's closed "Here is what is missing:" list does not say so. | `paycheck-engine.js:106-122` applies brackets to (gross minus state standard deduction) only. Oregon's own linked calculator page says the up-to-$8,750 federal-tax subtraction is NOT modeled, so estimates "run HIGHER". Applying it: Oregon state tax $8,177 to $7,412, net $70,403 to $71,168, spread $8,777 to $8,012, per-check gap $338 to $308, keep-rate floor 70.4% to 71.2%, and both JSON-LD blocks and the meta description move with them. Alabama: adding just its federal deduction ($13,170 x 5% = $658.50) moves net $74,370 to $75,029, rank 39 to 29, past 7 states. Asymmetric per state (Illinois models its exemption, New Jersey does not), so it reshuffles rather than cancels. | Yes (the ranking is new; the engine gap is not) |
| major | refute/copy | Lede overclaims: "the money that actually reaches your bank account ranges from $79,180 a year down to $70,403". No 401(k), HSA, health premium, local tax, one filing status, standard deduction only. Contradicted by the page's own FAQ ("Probably not exactly, and they are not meant to"). | Template line 62. Same pattern at build.js:5748, FAQ "How much of a $100,000 salary do you actually keep?", which enters FAQPage JSON-LD where Google can surface it stripped of caveats. | Yes |
| major | refute/copy | Local income tax, the exclusion that most distorts the ranking, is absent from the assumption box above the table and understated where it does appear ("Fourteen states let a city, county or school district charge income tax", which implies optional). | Assumption box, template line 73, lists only inclusions. Disclosure sits in bullet 3 below the 51-row table. Repo's own `state-payroll-2026.json`: Maryland "All 23 counties plus Baltimore City levy a local piggyback income tax... most large counties are at 3.20%"; Ohio "most cities levy 1.8%-2.75%". Maryland 35th at $74,642 becomes ~$71,442, 50th of 51, a 15-place drop applying to 100% of residents. Ohio 11th at $77,146 becomes $74,646, 35th, a 24-place drop. | Yes |
| major | refute/copy | Unsupported product claim: "it updates itself when a state publishes new tax brackets." Nothing in the repo ingests state tax data. | `tax-data-2026.json` is hand-maintained. The only writer of any tax-data file is `build.js:6229`, a dist copy. `scripts/check-sources.js` is a watcher polling one federal URL, self-described "best-effort". `check-freshness.js` only fails the suite. Even after a human edit, the page needs a rebuild and redeploy. Template line 116. The first half of the same sentence (table cannot drift from calculators, same engine, same data) was confirmed true. | Yes |
| major | refute/copy | The first analysis paragraph contradicts the page's own FAQ. It tells a reader weighing two no-tax-state offers that "the tax half of the question is already settled", while the FAQ says no-income-tax states "tend to collect more through sales and property taxes". | build.js:5680 versus the rendered FAQ answer on the same page. New Hampshire and Tennessee are both in the top tie and are about as far apart on property versus sales tax as two US states get. | Yes |
| minor | numbers | In 5 of 51 rows the published rounded columns do not add to the published take-home, off by $1. Display-rounding only; the underlying math is correct in all 5. | Indiana, South Carolina, Georgia, Rhode Island, District of Columbia. Each lands on an exact .5 in both the state-tax and the net figure (IN 2920.5 and 76259.5, DC 5531.5 and 73648.5, etc.), and each cell is independently rounded half-up by `usd0()`, so both round away from each other. The page states federal + FICA is $20,820 in every row, which invites the subtraction. | Yes |
| minor | sources | New York `_source` provenance is stale, citing SmartAsset/levyio and a 2025 page for figures New York has since published itself. No numeric discrepancy: all 27 thresholds and 3 standard deductions match. | NY has published Form IT-2105-I (2026) and NYS-50-T-NYS (1/26), both confirming our values. A future reviewer cannot tell the data is now primary-sourced. | No |
| minor | seo | 12 built non-embed pages carry a self-referencing canonical but are absent from sitemap.xml. | base-converter, base64-encode-decode, json-formatter, lorem-ipsum-generator, morse-code-translator, pomodoro-timer, random-number-generator, sleep-calculator, stopwatch, text-case-converter, time-zone-converter, uuid-generator. `git diff main...HEAD -- build.js \| grep sitemap` returns nothing; sitemap token count 21 on both branches. | No |
| minor | mobile | At 360px at rest the "Take-home a year" column is 87.9% visible and the first figure $79,180 is clipped by 17.2px until the reader swipes the table sideways. | Scroller spans x 14..346, first `td.net` rect left 221.2 right 363.2. Column visibility at rest: # 100%, State 100%, Take-home a year 87.9%, remaining three 0%. Screenshot `tbl-360-a.png` shows "$79,18" sheared. | Yes |
| minor | mobile | No sticky first column, so a phone reader scrolled fully right sees values with no state name attached. | At scrollLeft 482 (max), `tbl-360-b.png` shows seven consecutive rows reading "None  20.8%". No `position:sticky` on `td.rank` or the State cell. | Yes |
| minor | mobile | At 360px the three key stat tiles resolve to a 2-up grid, leaving the third alone with an equal-width empty gap. Cosmetic; nothing overflows or clips. | `gridTemplateColumns` = "160px 160px" at 332px content width. Tiles: best 14-174, worst 186-346 (row 1), gap 14-174 (row 2). `numOverflows` false on all three. | Yes |
| minor | refute/numbers | The lede's per-paycheck gap disagrees with the subtraction the page's own biweekly column invites. | Lede "about $338" from 8777.25/26 = 337.59. Table columns $3,045 minus $2,708 = $337.00 exactly. Mitigated by "about". | Yes |
| minor | refute/numbers | `og:description` reduces an 8-way tie to a singular "the best state". | Template line 10: "$8,777 more a year in the best state than in Oregon". Body correctly says "Eight states share the top of the table at $79,180" and the FAQ names all eight. Social preview only. | Yes |
| minor | refute/numbers | "None" in the state income tax column renders two different ways for the same fact. | Rows with programs emit `<td class="num tax">`, rows without emit `<td class="num tax zero">`. Washington's "None" is body colour, Texas's is muted via `.thpby table.thp td.zero { color:var(--muted) }`. Cosmetic. | Yes |
| minor | refute/copy | "Nothing on this page is typed in by hand" is literally false, and it opens the trust section. | Template line 116. The five analysis paragraphs, five FAQ questions, four assumption bullets, headings and `CMP_SOURCE_TITLES` are all hand-typed; only the figures inside them are computed. The next clause already says the accurate thing. | Yes |
| minor | refute/copy | The "Total tax rate" column is the cell a non-expert misreads, and its only gloss sits below a 51-row table that is itself a horizontal scroll region on a phone. Related: the Washington row reads "None + $807 paid family leave" under a header saying "State income tax". | Template line 100 (note placed after the table). Rendered Washington row: "10 Washington $78,373 $3,014 None + $807 paid family leave 21.6%". | Yes |
| minor | refute/copy | The built page ships 3 em dashes, all from pre-existing shared boilerplate, notably `og:image:alt` "Tools Berry — free online calculators, converters and 2026 tax tools". Also 4 new state titles contain an en dash (california, new-mexico, massachusetts) from the unchanged `stateRateFigure` helper. | `dist/take-home-pay-by-state/index.html` lines 50, 55, 60 contain U+2014 in build-injected meta. The same string appears 22 times in the untouched baseline. This branch neither introduced nor fixed it. An en dash is not an em dash, so the titles are not called a violation. | No (em dashes). Yes (en dashes, but from unchanged helper code) |
| minor | css | `npm run build` failed once with ENOENT on `dist/dependent-care-fsa-vs-credit-calculator/index.html`, then did not reproduce across two clean rebuilds. | First run errored at build.js:4730 (parent dir missing), launched concurrently with the wt-main build against a pre-existing partial dist. Likely a stale partial dist, not branch code. Not reproducible. | Unknown |
| minor | css | The stated build gate is not automatic in the way the brief describes: `package.json` `scripts.build` is exactly `node build.js`, and `verify-dist` is a separate script. | Contradicted in part by the gate verifier, which found `build.js` calls `verifyDist` internally at lines 16 and 6430 and prints on failure only. Unresolved between the two passes. Both pass when run explicitly. | No |
| minor | gate | Transient verify-dist failure caused by another session building in the same worktree. Both gates pass on a settled tree, but any gate result here has a shelf life. | At 23:30:00 a standalone verify-dist failed with 106 stale `/assets` 404 refs plus verify-dist's own "another session ran npm run build in this tree at the same time" diagnostic. build.log written 23:29:05 yet all 192 files in dist/assets carried mtime 23:30:00. Once no build.js process remained, verify-dist returned exit 0. | No |
| minor | bing audit, flagged separately | `dist/_headers` sets `X-Frame-Options: DENY` on `/*`, which also covers the 20 `/embed/*` pages that exist so third parties can iframe them. Embed pitches have already been sent; any partner who accepts finds the embed blocked. | build.js:5942. Not a crawler issue, so it was outside the audit's ranked list. | No |

## 5. What still needs a human

| Item | State | What is needed |
|---|---|---|
| Hacker News submission | Article written to `docs/hn-pdf-converter-writeup.md`, 1,381 words. NOT published, NOT submitted, no browser opened, no commit made. Untracked file; `build.js` does not read `docs/`, so the build gate is unaffected. | A ship/no-ship decision, plus resolution of 11 unverified claims (section 6). The article deliberately deviates from the brief: the brief ends at "filtering the chips cut conversion to ~4 seconds", but commit `5fdd492` and the `stencil_ocr.py` docstring show the chip filter made the file fast AND empty because body text was itself drawn as the same 2x2 chips masked by glyph stencils. The real fix was `stencil_ocr.py` (tesseract per merged stencil block, psm 6). The article is built around that reversal. |
| Ten .edu nudge emails | 10 of 10 drafted at `docs/edu-nudge-drafts.md`. `missing: []`. NONE SENT. | Send decision and send. |
| B3, URL cannibalization | Two live decisions on the table: ship as a pair, or fold one into the other. The new page was deliberately kept off the homepage because the homepage already carries two prominent links to the $75,000 study. | Edmond's call before deploy. |
| B1 and B2, tax data | Ohio $342 base amount, WA Cares 0.58%. Both pre-existing, both affect every paycheck estimate site-wide, not just the new page. | Decide whether to fix before ship or ship with the new page carrying the error into a ranked comparison. |
| Ohio 2026 disclaimer | Ohio asserts `figureYear 2026` with no visible banner while California, for the same situation, shows one. | Decide whether Ohio gets the same treatment as California. |
| CA and OK prior-year disclosure on the new page | The `/data/` sibling and the state pages both auto-emit a prior-year note; the new page does not. | Decide whether to port `priorYearNote` into the new page block. |
| IndexNow | Built and verified at `scripts/indexnow-submit.py`, key `9372e11bcbe34b0e993865299aae29dc` served and confirmed on disk. Entirely manual: `package.json` line 12 deploy script has no IndexNow step, no caller anywhere in the repo, the single GitHub workflow is a tax-freshness check that never deploys. With no args it re-blasts all 205 sitemap URLs, which is what earns the 429 the script already handles. | Decide whether to wire `&& python3 scripts/indexnow-submit.py <changed urls>` into the deploy script. Bing is currently the 10x traffic channel. |
| Bing audit recommendation 1 | 51 state pages ship 8 hardcoded `$0.00` strings inside `<div class="results">`, including `<div class="line total"><span class="lbl">Net pay</span><span id="rNet">$0.00</span></div>`. The wrapper is not hidden (no `display:none`, no `hidden` attribute), so a non-JS crawler renders "Net pay $0.00" while the correct $60,246 sits in prose. Template lines 243-251. | Decide whether to prerender the six values server-side from the same $75,000 default that already fills `ANSWER_LEAD` and `NET_BIG`. |
| Bing audit recommendation 2 | 51 of 51 state pages have valid JSON-LD (2 blocks each, 0 parse failures), but 0 of 51 contain the take-home dollar figure anywhere in structured data, and 0 of 51 declare a `WebApplication` node, while `tip-calculator` and `mortgage-calculator` both do. | Decide whether to add the figure to FAQ JSON-LD via the existing single-array generator. |
| Sitemap membership is manual | Nothing is auto-discovered. The sitemap comes from an explicit in-memory array (`build.js:3729`, roughly 200 `urls.push` calls). A new page builds, is reachable, and is absent from the sitemap unless someone adds a `urls.push`. No build error for forgetting. The new page WAS added correctly and is in the sitemap; this is a standing trap. | Awareness only. |

## 6. What could NOT be checked, and why

| Gap | Who reported it | Why |
|---|---|---|
| Ohio Revised Code 5747.02, the sole citation behind our Ohio 2026 data | sources | `codes.ohio.gov` and `legislature.ohio.gov` timed out or refused from both WebFetch (ECONNREFUSED 198.234.74.32:443) and curl (45s and 60s, IPv4 forced) over roughly 4 minutes. The verifier declined to substitute a blog or vendor page. Ohio's 2026 claim rests on an unverified citation. |
| WebSearch unavailable for the whole verification phase | sources, refute/numbers | Session WebSearch budget exhausted at 200 calls. All source URLs were reached directly instead. |
| 2026 Oregon federal-tax-subtraction phase-out threshold | refute/numbers | WebSearch exhausted. The $765.63 Oregon impact figure is computed from the site's OWN shipped $8,750 and is contingent on that subtraction being available at $100,000 single. Not independently confirmed. |
| Pixel-level mobile proof at 360px, by the build agent | build/dataPage | Headless Chrome ignored `--window-size` and laid out at roughly 600px, then cropped. The agent proved this was the harness (untouched production `/bmi-calculator/` clipped identically) and fell back to a CSS argument. **This gap was later closed**: the mobile verifier drove real headless Chrome 150.0.7871.187 over raw CDP with `Emulation.setDeviceMetricsOverride`, asserted `window.innerWidth` equalled the requested width at 360, 320 and 1200, and measured overflow directly. |
| Whether `npm run build` runs verify-dist | css versus gate | The two passes disagree. css: `package.json scripts.build` is exactly `node build.js` and verify-dist is a separate script, so the brief's claim is not true of this repo. gate: `build.js` calls `verifyDist` internally at lines 16 and 6430, silent on success. Not reconciled. Both exit 0 when run explicitly. |
| Live Cloudflare env values behind the HN article's rate-limit claim | authoring/hnWriteup | `functions/api/pdf-to-word.js` reads `UID_DAILY_LIMIT` from env with a fallback of 2 (IP 6, global 150). The deployed value is not in the repo. The article hedges to "a small number of conversions per user per day". |
| Which chip count is correct, 897 or 1,004 | authoring/hnWriteup | The repo contradicts itself for the same 19-page report: `lambda_function.py:66` says 897, the comment in `src/assets/pdf-to-word.js` says 1,004. The article used 897. Unresolved. |
| 40 versus 22 real pictures | authoring/hnWriteup | The same `lambda_function.py` comment block says "40 actual pictures" and, four sentences later, "all 22 real images intact". Likely occurrences versus distinct XObjects, but the repo never says so. The article reproduces both. |
| Whether "Word does not store cell shading as a colour" is true in general | authoring/hnWriteup | The repo asserts it as a general fact about Word, but the only evidence is one document that also went through a sanitizer/DLP pipeline, which `stencil_ocr.py` itself names as a possible producer. The sanitizer may be what created the chips. This is the most publicly checkable claim in the article and the most likely to be challenged. Needs a plain Word export with a shaded table before publishing. |
| Whether "4 seconds" is a measurement | authoring/hnWriteup | It is a code comment in `lambda_function.py`, not a measurement in the repo. Session memory (outside the repo, so unusable as a source under the brief) records 4.3s warm and 10.4s cold on prod. |
| Whether 600 dpi or 300 dpi describes the glyph mask | authoring/hnWriteup | `stencil_ocr.py` docstring says "a 600-dpi bitmap"; the module's own `DPI` constant is 300 (a different thing, the OCR render resolution). The article used 600 per the docstring. |
| The pypi.org link for pdf2docx | authoring/hnWriteup | Added by the author, not present in the repo, which only pins `pdf2docx==0.5.8`. Remove or verify before publishing. |
| 50 MB versus "no upload limit" | authoring/hnWriteup | `src/assets/pdf-to-word.js` sets `MAX_BYTES = 50 MB`, but shipped user-facing error strings in `functions/api/pdf-to-word.js` twice say "The in-browser converter has no upload limit". The code is authoritative; the live site currently tells users something different. |
| "Essentially all of the time inside ImagesExtractor.extract_images" | authoring/hnWriteup | Overstates the source. The repo comment says "Profiling put the time in ImagesExtractor.extract_images -> Collection.group, exactly there", does not quantify, and names an inner frame the article omits. |
| Timeout narrative | authoring/hnWriteup | The brief said 180s and timed out; the repo says it timed out at 60s and again at 180s, with the raise in commit `6ffef39`. The article followed the repo. `deploy.sh` is the source of truth for `TIMEOUT=180`, so a console-only change would be silently reverted; that was left out of the article as too operational. |
| The 2,068 figure versus the 897/1,004 figures | authoring/hnWriteup | 2,068 counts four pdf.js paint op types across the document, so it is paint operations, not distinct embedded images. The article describes it accurately but places it near the chip counts, where a reader may conflate them. |
| Whether any finding depends on another session's concurrent edits | authoring/bingAudit | At the audit's build time another session had uncommitted edits to `build.js` and `src/templates/state-page.html` in this worktree. Their diff touches no `urls.push`, no `$0.00` result line, and nothing named take-home-pay-by-state, so no bing finding depends on it. Stated, not silently assumed. |
| Whether the gate result survives | gate | The worktree is being mutated by other processes. Both gates pass on a settled tree, but whoever ships should confirm no other session is building before trusting a green gate. |
