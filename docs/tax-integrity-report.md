# Tax integrity report, branch `feat/authority-moves`

Branch HEAD `9f74e4d`, 4 commits off `main` (`b903205`). Build exits 0, `npm test` exits 0.

## 1. Ship verdict

**NO. Not safe to ship.** Five blockers, all independently reproduced:

| # | Blocker | Where |
|---|---------|-------|
| B1 | Alaska and Washington paycheck pages state in prose that "only federal tax and FICA come out" while the same page's calculator now subtracts a state deduction. Alaska's copy was TRUE on main and was made false by this branch. | `build.js:1608`, `build.js:1800-1803` |
| B2 | Ohio omits the statutory $332.00 base amount that the repo's own `_source` note quotes. Every Ohio filer above $26,050 has take-home overstated by $332 at every salary. Not introduced by this branch, but this branch publishes a second wrong Ohio number (the new $100k column) and a rank derived from it. | `src/data/tax-data-2026.json` `states.ohio` |
| B3 | Two em dashes (U+2014) introduced on added lines in net-new files. Absolute repo rule violation. | `scripts/proof-51-states.js:1`, `scripts/proof-diff.js:1` |
| B4 | New above-the-fold byline claims "every figure computed from published 2026 tax tables". False for California and Oklahoma, which carry `figureYear: 2025` and have no row-level marker. The branch replaced a true claim (`computed, not estimated`) with a false one. | `dist/data/take-home-pay-by-state/index.html:104` |
| B5 | The study table's sticky column headers never pin. On a phone the two money columns become indistinguishable after the first screenful. Causation proven in-page. | `table.study thead th` inside `.table-scroll{overflow-x:auto}` |

## 2. Tax corrections, one row per state investigated

| State | Confirmed | Refuter verdict | Applied | Effect at $100k (annual net) | Official source |
|-------|-----------|-----------------|---------|------------------------------|-----------------|
| Washington | yes | upheld | yes | -$580.00 (WA Cares 0.58%, uncapped) | https://paidleave.wa.gov/employer-roles-responsibilities/ |
| Minnesota | yes | upheld | yes | -$440.00 (MN Paid Leave 0.44%, base $184,500) | https://pl.mn.gov/resources/calculators/premium-rate-and-contributions |
| Alaska | yes | upheld | yes | -$271.00 (AK SUI 0.5%, base $54,200, cap binds) | https://labor.alaska.gov/estax/2026-experience-rates.html |
| New Jersey | yes | upheld | yes | -$190.40 (UI 0.3825% + WF/SWF 0.0425%, base $44,800; TDI/FLI unchanged) | https://www.nj.gov/labor/ea/employer-services/rate-info/ |
| Pennsylvania | yes | upheld | yes | -$70.00 (PA UC 0.07%, uncapped) | https://www.pa.gov/agencies/dli/resources/for-employers-and-educators/how-to-file/uc-tax/calculating-contributions--penalties-and-interest |
| Delaware | yes | upheld | reference file only | $0.00 by design. The 0.4% is a ceiling reachable only at employers with 25+ DE employees, and the employer may lawfully pay 100%, so no `employeePrograms` entry was added. Applied: start-date correction (deductions began Jan 1 2025, not 2026), headcount qualification, law-firm-blog source replaced with the .gov page. | https://labor.delaware.gov/delaware-paid-leave/ ; https://delcode.delaware.gov/title19/c037/index.html |
| Ohio | yes (by proposer) | **REFUTED** | no | $0.00 applied. Would have been -$332.00 if the proposed base had shipped. Refuted because the proposed $332.00 base is contradicted by ODT's own tax-year-2026 IT 1040 ES worksheet, which prints **$342.00** plus 2.750% over $26,050 AND still shows a $100,000 band ($2,394.32 + 3.125%), which the proposal claimed does not exist in 2026. The engine also has no concept of Ohio's $1,900-per-exemption step, so adding a base alone would manufacture a phantom cliff for low earners. | https://codes.ohio.gov/ohio-revised-code/section-5747.02 (proposer, unreachable from this environment for re-verification) |

Ohio's refutation is a result, not a clearance. The number currently shipped is still wrong (see B2). The refutation established only that the *proposed replacement* was also wrong.

### Movement, all 51 jurisdictions, $100,000 single filer biweekly

| Moved | Did not move | Unapproved states that moved |
|-------|--------------|------------------------------|
| 5 (AK, MN, NJ, PA, WA) | 46 | **0** |

Arithmetic reconciliation of each delta was re-derived independently and matches the approved source figures exactly.

## 3. Verification passes

| Verifier | Verdict | Single most important number measured |
|----------|---------|----------------------------------------|
| gate | pass | `npm run build` exit 0 and `npm test` exit 0, real exit codes. 242 `.html` files emitted; 962 lines of test output, 0 failing suites. |
| recompute | pass (1 minor) | 102 of 102 published rows matched an independent reimplementation across 6 fields each, 1,530 field comparisons, 0 mismatches, in JSON, CSV and rendered HTML. |
| nomove | pass | 9,945 input combinations swept (51 states x 3 filing statuses x 5 frequencies x 13 salaries incl. every wage-base boundary). Unauthorised movers at any input: 0. Engine file byte-identical between trees. |
| blast | **fail** | 55 of 242 built pages show dollar-figure changes; 24 neighbour-table rows changed across 20 pages, every one attributable to an authorised state. |
| invariants | pass | 252 external hosts on main, 251 on branch. New hosts: **0**. AdSense loader: 218 pages both trees, exactly one per page. `<ins class="adsbygoogle">`: 0. `styles.css` byte-identical, same content hash `styles.81ca39ca81.css`. |
| seo | pass (1 minor) | 51 of 51 state titles carry the correct `<state> paycheck calculator` phrase, 0 duplicate titles sitewide, 537 JSON-LD blocks parsed, 0 failures. |
| mobile | **fail** | At 360px the table scroller is 332px wide with `scrollWidth` 900 (2.71x). Only 3 of 8 columns reachable at rest. |
| attack: numbers | **fail** | Alaska prose implies $61,592.50; the calculator on the same page computes $61,321.50. Gap $271.00. Washington gap $1,040.37 at $75k, rising to $4,389.21 at $500k. |
| attack: copy | **fail** | Exactly 2 em dashes on added lines, both in net-new `scripts/` files. Commit messages clean (0 across 4 subjects and bodies). |

## 4. All failures and attack findings, by severity

### Blocker

| ID | Finding | Evidence |
|----|---------|----------|
| F1 | **AK and WA pages assert only federal + FICA is withheld while the calculator subtracts a state deduction.** Alaska's contradiction is new in this branch. | Alaska lede: "Alaska runs on oil revenues and the Permanent Fund, not a wage tax, so just federal tax and FICA come out." Body: "the only deductions on your 2026 paycheck are federal withholding and FICA, no state line at all." Same page: "the AK SUI (employee) contribution is subtracted from the take-home estimate at the top of this page." Implied $61,592.50 vs computed $61,321.50. Washington: "your paycheck loses only federal income tax and FICA" vs computed $60,552.13, a $1,040.37 gap at $75k. `build.js:1608` (stateLede) and `build.js:1800-1803` branch on `state.hasIncomeTax` and never read `state.employeePrograms`. |
| F2 | **Ohio understates state tax by the $332 statutory base amount, and the data file calls that base a "rounding offset".** | `ohio._source` says "the tax is $332.00 plus 2.75% of the amount in excess of $26,050 ... Modeled as marginal brackets (the engine reproduces marginal tax, not Ohio's small base-amount rounding offset)". Encoded brackets: `[{rate:0,upTo:26050},{rate:0.0275,upTo:null}]`. Computed at threshold: $26,050 -> $0.00; $26,100 -> $1.38 (schedule gives $333.38). At $75,000 we compute $1,346.13 vs $1,678.13, a 19.8% understatement of the whole Ohio bill. ODT's 2025 booklet worked example proves the base is additive ($68,050 -> "$1,155 + $342 = $1,497"). Ohio rank moves 11 -> 12 at both salaries if corrected. The file's stated reason for dropping the top band is also factually wrong: it calls 3.125% the "superseded pre-HB 96 ladder", but the pre-HB 96 2024 schedule was 3.50% at that base. Exposure at $200,000 Ohio: $4,783.63 computed vs $5,519.32 on ODT's 2026 worksheet, a $735.69 gap. |
| F3 | **Two em dashes introduced on added lines in net-new files.** | `scripts/proof-51-states.js:1` = `// proof-51-states.js — one-off proof harness for the authority-moves data pass.` `scripts/proof-diff.js:1` = `// proof-diff.js — diffs two proof-51-states.js snapshots and prints a table.` Both files absent from `wt-main`. Complete non-ASCII inventory of added lines: 2x U+2192, 2x U+2014, 2x U+00B7. A third at `docs/authority-moves-report.md:83` inside a quoted string. The branch's own ship report scanned the built page and the titles for dashes but never scanned `scripts/`, and self-reported "No". |
| F4 | **Byline claims all figures come from published 2026 tables. False for 2 of 51 rows, and the page contradicts itself ~2,000 words later.** | Built line 104: `All 50 states + DC · every figure computed from published 2026 tax tables`. `wt-main` line 78 read the accurate `All 50 states + DC · computed, not estimated`. `tax-data-2026.json` has `california: figureYear 2025` and `oklahoma: figureYear 2025`. The page's own limits section admits it. Neither row carries a marker: `<tr id="state-california"><td class="rank" data-val="47">47</td>...` has no year flag, no asterisk, no title attribute. California is ranked 47 and gets a named analysis paragraph built on a 2025 bracket set. |
| F5 | **Study table sticky headers never pin.** | `table.study thead th{position:sticky;top:0}` is inert because `.table-scroll{overflow-x:auto}` makes that div the scrollport and it has no vertical overflow. Measured at 360px, scrollY 2940: `thead th` `getBoundingClientRect().top = -599.6px`, pinned false. Causation proven in the same page instance: setting `.table-scroll{overflow:visible}` at the identical scrollY gives `top = 0.0px`, pinned true. Screenshot `shot-360-sticky.png` shows rows 11 to 25 with no headers; the visible column reads `$60,246` with nothing saying which salary. Secondary: even fixed, `top:0` pins under `header.site` (sticky, `top:0`, `z-index:20`, 107.44px tall) while the `th` has `z-index:auto`. |

### Major

| ID | Finding | Evidence |
|----|---------|----------|
| F6 | AK, PA and WA pages describe the new deductions as "disability / paid-leave contributions". They are unemployment insurance and long-term care. Pennsylvania has no state disability or paid-leave program at all. | `build.js:1713` emits ` and ${state.abbr} disability / paid-leave contributions` whenever `employeePrograms` is non-empty. `dist/pennsylvania-paycheck-calculator/index.html`: "...and PA disability / paid-leave contributions", while the same page's table correctly reads "+ $53 unemployment insurance". Pages carrying the phrase: 11 on main, 14 on branch. NJ is a partial case (TDI/FLI correct, UI and WF/SWF not). MN is the only accurate one. |
| F7 | Delaware's employee-paid PFML premium is silently excluded from the ranked study while the methodology presents the payroll layer as complete. Three pages on this site disagree about whether Delaware withholds. | `state-payroll-2026.json` documents "Delaware Paid Leave (PFML) - employee share" up to 0.4% capped at $184,500. `tax-data-2026.json` has no `delaware.employeePrograms`. Study prints Delaware $57,874 rank 42 at $75k and $73,811 rank 45 at $100k; at the statutory 50/50 default it is $57,573.50 rank 48 and $73,411.00 rank 47, a six-place shift. Methodology says "Employee-paid state premiums in the 14 jurisdictions that have them" and enumerates 14 without Delaware. "What this does not include" never mentions it. `/what-applies-to-me/` and the Delaware paycheck page both say "Not in our take-home estimate". `dist/data/take-home-pay-by-state-2026.json` `assumptions.excludes` omits it too. A programmatic diff of `payrollContributions` vs `employeePrograms` across all 51 states returns Delaware as the only remaining gap. |
| F8 | WA Cares is the only one of the five additions with a large permanently-exempt population, and the only one given no eligibility caveat. Its copy actively asserts universality. | The other four all carry caveats written in the same commit (AK: reimbursable-employer exemption; MN: "employer must pay at least half and may choose to pay more"; DE: full headcount table; NJ: governmental lower rate). WA Cares `_fullName`: "fully employee-paid; no wage cap and no Social Security cap, so it applies to every dollar of gross wages"; page table: "No wage cap (applies to all wages)". Workers with pre-2021 qualifying private LTC insurance, out-of-state workers, non-immigrant visa holders, military spouses and disabled veterans hold exemptions. For any of them we now deduct $435.00 at $75k, $580.00 at $100k, $2,900.00 at $500k. |
| F9 | Take-home changed on 5 states and rankings shifted on 9 more, plus the study, CSV, JSON, `llms-full.txt`, homepage and `/what-applies-to-me/`. None of this is in the authorised change list, which named only the 51 titles/h1s, the `/data/` page and the removal. | At $75k: AK 61,593 -> 61,322 (rank 1 -> 9), WA 60,987 -> 60,552, MN 58,016 -> 57,686 (rank 42 -> 48), NJ 58,626 -> 58,436 (rank 32 -> 34), PA 59,290 -> 59,238. Nine further states (ND, GA, OK, DE, CT, NY, MA, KS, CA) shift rank as a knock-on. The numbers cross-check clean against JSON, CSV and HTML; the scope does not match the authorisation. |
| F10 | The published CC-BY dataset changed schema in a breaking way and the CSV changed column order. Existing consumers break silently. | Top-level `states` removed, replaced by `salaryLevels[]`; `assumptions.grossSalary` (number) becomes `assumptions.grossSalaries` (array), so `require(...).states` returns undefined. CSV header goes from `Rank,State,Abbr,Gross salary,Annual take-home,...` to `Gross salary,Rank,State,Abbr,Annual take-home,...`, so a positional parser reads a rank where it expects a salary. Both are advertised as stable schema.org `DataDownload` endpoints under CC-BY 4.0, and the backlink tracker records this study as pitched to .edu recipients. No versioning, no legacy key retained. |
| F11 | The ranked table is the page's product, and the page admits the ranking is materially wrong for 14 of 51 rows, with no row-level marker. | Page text: "14 states allow city, county or school-district income taxes that are not modelled here: Alabama, Delaware, Indiana, Iowa, Kentucky, Maryland, Michigan, Missouri, New Jersey, New York, Ohio, Oregon, Pennsylvania and West Virginia." That sits far below the table and below the ads. Using the repo's own documented rates: Maryland shown 36th at $74,642 becomes roughly 50th of 51 at the 3.20% largest-county rate; Ohio shown 11th becomes ~35th at Columbus 2.5%. |
| F12 | Only 3 of 8 columns are reachable at rest on a phone, and there is no sticky state-name column. | At 360px the `#thpTable` scroller is 332px wide, `scrollWidth` 900. Column offsets: # 0, State 86.9, Take-home $75k 221.3, Take-home $100k 330.8, State tax $75k 440.2, State tax $100k 546.8, Total rate $75k 653.4, Total rate $100k 776.7. The $100k take-home column peeks by 1.2px; the four tax columns are fully off-screen. Reaching "Total tax rate on $100,000" needs `scrollLeft >= 568`, at which point the State column (right edge 221.3) is gone. `shot-360-table-right.png` shows a full screen of figures with no state names and no headers. |
| F13 | 14 of 51 rows render as tall unexplained blank voids on a phone, because the sub-line causing the height sits in a column that is never on screen. New on this branch (`3f2701e`; `td.tax .sub` does not exist in main's template). | Median row height at 360px is 41.3px. Rows with `td.tax .sub`: New Jersey 185.4, Rhode Island 144.3, New York 144.3, Washington 123.7, Alaska 103.1, Pennsylvania 103.1, California 103.1, Hawaii 103.1, plus six at 82.5. The `.sub` box is 86.6px wide so "+ $505 disability insurance, paid family leave and other state programs" wraps to 144.2px of text height. That text lives at scroller offset 440.2px. `shot-360-table.png` shows Alaska and Washington as large gaps. |
| F14 | Two of the four headline figures fail WCAG AA contrast in dark mode. Pre-existing on main, but this branch doubled the affected cards from 4 to 8. | Computed in-page at 360px with `prefers-color-scheme:dark`: `.stat-card.low .n` = rgb(180,35,24) on rgb(22,29,39) = **2.58:1**; `.stat-card.same .n` = rgb(29,78,216) on the same = **2.53:1**. Both 29.6px weight 800, so the large-text AA floor is 3:1. Both fail. Cause: hardcoded `#b42318` and `#1d4ed8` in the page's own `<style>`, chosen against a white card, never overridden for dark. `.stat-card.high .n` uses `var(--accent)` and passes at 6.66:1. |

### Minor

| ID | Finding | Evidence |
|----|---------|----------|
| F15 | Delaware is the only state with a documented employee-paid contribution and no `employeePrograms` entry, while MN and MA, whose sources carry the same "up to / employer may pay more" language, are modelled at full employee share. Consistency gap, pre-existing. | Same evidence as F7. MN Paid Leave modelled at full 0.44% ("employer may deduct up to half"); MA PFML at full 0.46% ("Up to 0.46%"). |
| F16 | Study says North Dakota is "exactly $203 behind" while printing two numbers that subtract to $204. | Table cells $61,593 and $61,389 differ by $204. Unrounded engine values $61,592.50 and $61,389.21 differ by $203.29. The word "exactly" is what breaks it. |
| F17 | Section 125 pre-tax deductions do not reduce the base for any state employee program, so advanced mode over-withholds every newly added premium. Pre-existing, but the two new uncapped programs make it bite at every income. | `stateEmployeePrograms(grossAnnual, stateData)` is called with `grossAnnual`, never `grossAnnual - cafeteria125`, while `federalIncomeTax`, `ficaTax` and `stateIncomeTax` all take a `preTax` argument. At $75k with $6,000 Section 125: WA charges $1,040.37 vs $957.14 (+$83.23), MN $330.00 vs $303.60 (+$26.40), PA $52.50 vs $48.30 (+$4.20). Wrong direction: overstates the deduction. |
| F18 | H1 and JSON-LD headline call DC a state, contradicting every other string on the page. | Line 98: `<h1>Take-home pay on $75,000 and $100,000 in all 51 states, compared (2026)</h1>`; JSON-LD headline matches. Byline says "All 50 states + DC", lede says "all 50 states and the District of Columbia", H2 says "all 51 jurisdictions". Pre-existing wording, but this branch rewrote that headline and left it. |
| F19 | "Open calculation engine" implies inspectable source the page never offers. | No link to source in the section. Repo is `github.com/edyda99/tools-cherry` with no public-source claim linked. |
| F20 | Table footnote buries a genuine sorting trap behind five program names. | "...on top of or instead of income tax; sorting a state tax column sorts on the income tax alone." California's cell visibly reads "$5,208 + $1,300 disability insurance" but the column sorts on 5208, so sorted order contradicts the numbers on screen for all 14 premium states. |
| F21 | On-page differentiation across the 51 state pages went down. | All 51 h1s are now exactly `<State> Paycheck Calculator` (0 exceptions). Main had two patterns. Title suffix distribution after "2026: ": 30 pages "Take-Home Pay After Taxes", 16 "Take-Home Pay", 5 state-specific. Titles remain unique via the state name, so not a duplicate-content defect, but it works against the recorded state-skeleton-break effort. Not stuffing: no repetition, no comma-stacked keyword lists, each phrase appears once. Weakest: Wyoming and Virginia ("take-home pay" already means "after taxes", and Wyoming has no state income tax), and "District of Columbia Paycheck Calculator 2026: Take-Home Pay" (60 chars, reads as a truncated stub). |
| F22 | Undeclared substantive changes to `/what-applies-to-me/` source data, including a factual correction and a citation swap. | `state-payroll-2026.json` rewrites the Delaware PFML entry (tiered by employer size) and changes the distinctive fact from "took full effect Jan 1, 2026 ... with a new employee payroll deduction" to "payroll deduction began Jan 1, 2025, not 2026", swapping the source from `ebglaw.com` to `labor.delaware.gov`. Two PA UC source URLs also swapped. Genuine corrections, undeclared. |
| F23 | Bonus-tax calculators ship the enlarged tax-data payload but ignore it, so the same state gives inconsistent answers across two tools. | 9 bonus pages differ; the only change is the inlined `window.__BONUS_TAX__` blob. `src/assets/bonus-tax-calculator.js` contains no reference to `employeePrograms`, `statePrograms` or `payrollDeduction`, and `dist/assets` is byte-identical between builds. `/washington-paycheck-calculator/` now subtracts WA Cares; `/washington-bonus-tax-calculator/` does not. Pre-existing gap (CA SDI already ignored), widened to 5 more states. |
| F24 | The branch's own report cites line numbers in a `dist` file the build does not produce. | `docs/authority-moves-report.md:83` cites `dist/take-home-pay-by-state/index.html` lines 50, 55, 60. No such file exists in either build; the page is at `dist/data/take-home-pay-by-state/`. `docs/bing-ai-surface-audit.md:70-79` reasons about the same root-level path. Neither doc ships. |
| F25 | At exactly 360px the headline figures have zero headroom; one more character would overflow the card box. | Grid resolves to `160px 160px`, 128px content width. `.stat-card .n` measures `scrollWidth` 128 vs `clientWidth` 126 on 6 of 8 figures; "$61,593" natural width 127.56px. An 8-character figure at 29.6px/800 measures ~145px, ~17px past the box, and `white-space` is `normal` with no break opportunity inside "$100,700". Clears at 320px and at 390px+. |
| F26 | No visual affordance that the main table scrolls sideways. | At 360px the next column peeks by 1.2px out of a 332px scroller, so the table reads as if it ends at "Take-home on $75,000". |
| F27 | 11 page titles exceed 60 characters. | All 11 are `/embed/*`, all carry `<meta name="robots" content="noindex, follow">`, none appear in `sitemap.xml`. Longest: `dist/embed/w2-box-decoder/index.html` at 80 chars. No indexable page affected. |

## 5. What could NOT be checked

| Gap | Why |
|-----|-----|
| Ohio's 2026 statutory base against a primary source, this session | `codes.ohio.gov` refused the connection, `tax.ohio.gov` returned 404 on two paths, `law.justia.com` returned 403, and the web-search budget was exhausted. F2 is argued entirely from internal contradiction between `ohio._source` prose and `ohio.tax.brackets` in our own file, which holds regardless of which external figure is right. ODT publishes no 2026 row on its Annual Tax Rates page (it states it covers 2005 through 2025), so the underlying question is genuinely open. |
| Washington Pension Funding Council 2026 rate adoption | No PFC rate-setting document for the Jan 1 2026 biennial review was located; `leg.wa.gov` OSA and PFC URLs 404 and the search budget ran out. The 0.58% rests on the RCW 50B.04.080 ceiling of "no greater than .58 percent" plus two official state sites currently publishing it. The rate cannot be lawfully higher; a council decision to set it LOWER would not be visible in the cited sources. This is the least-supported of the five figures shipped. |
| WA Cares exemption statute, re-verification | The exemption classes in F8 could not be re-verified this session (search budget exhausted). The disclosure asymmetry against the other four states in the same commit stands independently. |
| Minnesota agency wording, direct fetch | Every `mn.gov` and `pl.mn.gov` URL returned HTTP 403 (Radware bot protection). The 0.88% total was read only from a search-result snippet plus one independent article. The load-bearing numbers (0.44% employee share, FICA-linked cap) come from Minn. Stat. 268B.14 read in full. The 0.61% medical / 0.27% family split in the proposal prose was NOT verified and was correctly left out of the shipped entry. |
| Pennsylvania 2026 employee rate on a second `.gov` page | Only one `.gov` page states 0.07%. The year-specific 2026 confirmation comes from Bloomberg Tax (10 Nov 2025) and Abacus Payroll, not a second government source. The rate is re-announced every July 1 under Sec. 301.7/301.8 and can lawfully be 0.0%, so it needs a live re-check every tax-year roll. Do not write "and thereafter" into any comment. |
| Real iOS overlay-scrollbar behaviour | Headless Chrome draws classic scrollbars. The 1.2px column peek in F26 is measured; how a fading overlay scrollbar reads on a real device is not. |
| Tax-correctness sign-off on the wider blast radius (F9) | The numbers cross-check internally across HTML, CSV and JSON, and the recompute verifier reproduced all 102 rows independently. What was NOT performed is an authorisation check that the 9 knock-on rank shifts and the derived narrative claims were intended to change in this pass. |
| Whether Delaware's omission is the right call | Verified: Delaware is the only documented-but-unmodelled state. NOT verified: whether the study's methodology text and `assumptions.excludes` should disclose it. That is a judgement call, not a measurement. |

## 6. What still needs a human

| # | Decision |
|---|----------|
| 1 | **Ohio.** Two contradictory official figures ($332 from ORC 5747.02 per the proposer, $342.00 + a live $100,000 band from ODT's tax-year-2026 IT 1040 ES worksheet), plus a $1,900-per-exemption step the engine cannot represent. Shipping either number without the exemption step manufactures a cliff for low earners. Needs Edmond to pick the source of truth and decide whether the engine gains base-amount and exemption support. |
| 2 | **Whether to fix the AK/WA prose or revert the AK/PA/WA data.** F1 and F6 are both fixable in `build.js` copy, but the decision on which is authoritative (the calculator or the page) is Edmond's. |
| 3 | **The published dataset schema break (F10).** The CSV/JSON are CC-BY endpoints pitched to .edu recipients. Whether to version, retain the legacy `states` key, or break outright is a distribution decision. |
| 4 | **Delaware disclosure (F7).** Either model it with a headcount caveat, or add it to the study's `excludes` list and the methodology text. Doing neither leaves three pages on the site disagreeing. |
| 5 | **The undeclared blast radius (F9) and the undeclared `state-payroll-2026.json` edits (F22).** Both look correct; neither was in the authorised scope. |
| 6 | **Washington 0.58% (see section 5).** Accept the statutory-ceiling argument or block until a PFC document is located. |
| 7 | **The 14 local-income-tax states (F11).** Whether a ranked table may ship with a disclosed-but-unmarked ordering error affecting 27% of rows. |
